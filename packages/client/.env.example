# Optional: USDA FoodData Central API Key (falls back to DEMO_KEY if not provided)
# Get a free key at: https://fdc.nal.usda.gov/api-key-signup.html
VITE_USDA_API_KEY=DEMO_KEY

# Optional: Backend API URL (defaults to http://localhost:3000 in dev; falls back to localStorage in production/standalone)
# VITE_API_URL=https://your-backend-api.com


